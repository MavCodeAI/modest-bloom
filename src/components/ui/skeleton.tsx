// Loading placeholders are intentionally disabled across the app:
// content simply appears once it is ready, with no shimmer or spinner.
function Skeleton(_props: React.HTMLAttributes<HTMLDivElement>) {
  return null;
}

export { Skeleton };
